# Use this profile when running locally without Redis:
#   mvn spring-boot:run -Dspring-boot.run.profiles=local
spring:
  cache:
    type: none
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

cache:
  enabled: false
